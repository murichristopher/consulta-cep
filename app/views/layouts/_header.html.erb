<header class="flex items-center justify-between p-4 bg-base-100 shadow-md">
  <div class="flex items-center space-x-2">
    <%= heroicon "magnifying-glass-circle", options: { class: "h-6 w-6 text-primary" } %>
    <h1 class="text-2xl font-bold text-primary">ConsultaCep</h1>
  </div>

  <button class="btn btn-primary flex items-center space-x-2"
          onclick="document.getElementById('my_modal_1').showModal()">
    <%= heroicon "cog-6-tooth", options: { class: "w-5 h-5" } %>
    <span><%= t('modal.open_config') %></span>
  </button>
</header>